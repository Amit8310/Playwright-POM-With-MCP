// const { test, expect } = require('@playwright/test');

// test('homepage has title and intro', async ({ page }) => {
//   await page.goto('https://playwright.dev');
//   await expect(page).toHaveTitle(/Playwright/);
//   await expect(page.locator('text=Fast and reliable end-to-end testing for modern web apps')).toBeVisible();
// });
